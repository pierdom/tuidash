services:
  tuidash:
    image: ghcr.io/pierdom/tuidash:latest
    env_file:
      - .env
    environment:
      - TUIDASH_SERVE_URL=http://localhost:8080
    ports:
      - "8080:8080"
    restart: unless-stopped
